@model MovieSearch

@{
}

<p>
    <form asp-action="MovieSearch">
        <label asp-for="Title" class="control-label"></label>
        <input asp-for="Title" class="form-control" />
    </form>
    <button class="btn btn-primary" type="submit">Submit</button>
</p>